function fetchUserdata() {
    const apiURL = 'https://jsonplaceholder.typicode.com/users';
    document.getElementById('api-data').style.display = 'datacontainer';

    async function fetchUserdata(apiURL) {
        try {
            const response = await response.json(); 
        }
}
  dataContainer.innerHTML = 'Loading user data...';

  <ul>
    <li>user's name</li>
    <li>userList</li>
  </ul>
  datacontainer.style.display = 'userList';

  dataContainer.innerHTML = 'Failed to load user data.';

  document.addEventListener('DOMContentLoaded', fetchUserdata);atch(apiURL);